/**
 * Задача: deepCount — число всех элементов с учётом вложенных массивов (Сбер / подготовка).
 */

// =============================================================================
// Задача
// =============================================================================
//
// Дан массив. Написать функцию, которая возвращает количество всех элементов массива,
// включая любые вложенные массивы.
//
// Примеры:
//   []                    --> 0
//   [1, 2, 3]             --> 3
//   ["x", "y", ["z"]]     --> 4
//   [1, 2, [3, 4, [5]]]  --> 7
//
// console.log(deepCount([]));
// console.log(deepCount([1, 2, 3]));
// console.log(deepCount(['x', 'y', ['z']]));
// console.log(deepCount([1, 2, [3, 4, [5]]]));

// =============================================================================
// Решение и объяснение
// =============================================================================
//
// Каждый не-массив даёт +1. Каждый вложенный массив считается как отдельный элемент (+1)
// плюс всё, что внутри него (рекурсия). Так совпадают с примерами: у ["z"] массив даёт 1,
// "z" ещё 1.
//
// Сложность: O(n) по общему числу «узлов» (значений и вложенных массивов) — за один обход
// дерева вложенности.

/**
 * @param {unknown[]} arr
 * @returns {number}
 */
function deepCount(arr) {
  let result = 0;
  for (let i = 0; i < arr.length; i++) {
    if (Array.isArray(arr[i])) {
      result += 1 + deepCount(arr[i]);
    } else {
      result++;
    }
  }

  return result;
}

// console.log(deepCount([])); // 0
// console.log(deepCount([1, 2, 3])); // 3
// console.log(deepCount(['x', 'y', ['z']])); // 4
// console.log(deepCount([1, 2, [3, 4, [5]]])); // 7
