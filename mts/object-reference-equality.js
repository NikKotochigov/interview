/**
 * console.log({ a: 1 } == { a: 1 })  →  false
 *
 * Операторы == и === для объектов сравнивают ссылки (один и тот же экземпляр в памяти),
 * а не «содержимое» по полям. Каждый литерал { a: 1 } создаёт НОВЫЙ объект, поэтому
 * это два разных объекта — равенства нет.
 *
 * Сравнение «по структуре» делают вручную (рекурсия, isEqual из lodash) или через
 * structuredClone/сериализация — в зависимости от задачи.
 */

// console.log({ a: 1 } == { a: 1 }); // false
// console.log({ a: 1 } === { a: 1 }); // false

const x = { a: 1 };
// console.log(x === x); // true — одна и та же ссылка
